OCAMLMAKEFILE=OCamlMakefile
RESULT=ocamltest

PREDS=camlp4o
ANNOTATE=yes
PACKS += unix lwt.unix
PACKS += lwt.syntax
PACKS += yojson

SOURCES=cref.mli cref.ml cmd.ml main.ml

all: nc
	@ :

-include $(OCAMLMAKEFILE)
